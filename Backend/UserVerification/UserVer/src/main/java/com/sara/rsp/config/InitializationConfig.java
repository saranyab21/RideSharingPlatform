package com.sara.rsp.config;

/*import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.ApplicationArguments;
import org.springframework.boot.ApplicationRunner;
import org.springframework.context.annotation.Configuration;
import com.sara.rsp.service.*;


@Configuration
public class InitializationConfig implements ApplicationRunner {
    private final AuthUserService authUserService;

    @Autowired
    public InitializationConfig(AuthUserService authUserService) {
        this.authUserService = authUserService;
    }

    @Override
    public void run(ApplicationArguments args) throws Exception {
        // Call createKnownUsers() in the run method
        authUserService.createKnownUsers();
    }
} */

